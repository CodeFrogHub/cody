describe "Model Group", ->
  context "attributes", ->
    it "should be required"
    it "should be unique"
    it "should belong to"
    it "should have many"

  context "object method", ->
    it "should save"
    it "should do something and save"
  context "collection method", ->
    it "should find all where"
    it "should find one where"
    
